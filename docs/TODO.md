* add more dynamic colors to the volumes
* check Jmol viewer works
* add animated GIF to report page
* convert all vossvolvox function to fast mode
* possibly switch from mariadb to sqlite / keep or drop sinedon
* have webpage open by default at localhost:8080/ not localhost:8080/php/index.html
* print message at end of build to open the page localhost:8080/php/index.html
* provide more examples on the pages
* links to my social media
* email Gerstein lab
* add PDB to images of cavities and channels
* provide more PDB processing choices
* test the 3d printng codes
* allow all functions to input PDB directly, skip the pdb_to_xyzr step
* test runtimes, and perhap allow smaller grids on webpage
* add confirmation about sharing results, or default to NOT share results on PDB upload
* look at code to avoid text injections, like with probe or pdb entry. sanitize the input.
* provide a link to submit bugs to github tracker
* convert backend to rust
